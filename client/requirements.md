## Packages
framer-motion | Page transitions and scroll-triggered animations
recharts | Dashboard analytics and progress visualization
date-fns | Human-readable date formatting
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging tailwind classes without style conflicts

## Notes
- Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
- The app uses a dark mode, performance-athletic aesthetic with volt green accents.
- Replit Auth is used for user sessions.
